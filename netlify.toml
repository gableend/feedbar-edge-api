[build]
  command = "npm run build"
  publish = "dist"
  functions = "dist/netlify/functions"

[functions]
  node_bundler = "esbuild"

# ✅ FIX: Point to the new filename "ingest-background"
[functions."ingest-background"]
  schedule = "*/10 * * * *"